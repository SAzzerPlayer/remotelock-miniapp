import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  flexWrapper: {
    flex: 1,
    flexDirection: "row",
    justifyContent: "flex-end",
    alignItems: "flex-end",
    marginBottom: -8,
  },
  lockIcon: {
    width: 48,
    aspectRatio: 1,
    marginRight: 8,
  },
  avatar: {
    borderWidth: 3,
  },
});
